<MudDialog>
    <DialogContent>
        <MudText>@Context.Name</MudText>
        <MudText>@Context.Subject</MudText>
        <MudText>@Context.Population</MudText>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Отменить</MudButton>
        <MudButton Color="Color.Secondary" Variant="Variant.Filled" OnClick="Delete">Удалить</MudButton>
    </DialogActions>
</MudDialog>

@code { 
    
    [CascadingParameter] MudDialogInstance MudDialog { get; set; }

    [Parameter] public NationalityViewModel Context { get; set; }

    void Delete() => MudDialog.Close(DialogResult.Ok(Context));
    void Cancel() => MudDialog.Cancel(); 
}